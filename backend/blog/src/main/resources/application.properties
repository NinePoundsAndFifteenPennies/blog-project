spring.application.name=blog

# datasource settings
spring.datasource.url=jdbc:mysql://localhost:3306/blog?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=437179745

# jpa settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Settings
# ???????????????????????????????????????
app.jwt.secret=DFoin7uuuVzunje2OpBpZBWdTFRKrU0ran1U6zu2fuk3mfRyqzzLUTXvGDZkK1xGg3Xwyz9Eo398H1tpXkjtWA==
# Token???????????????1??
app.jwt.expiration-ms=3600000
# "记住我"Token???????????????30??
app.jwt.remember-me-expiration-ms=2592000000

# File Upload Settings
file.upload-dir=uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Comment Settings
comment.max-nesting-level=3

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0
spring.data.redis.timeout=2000ms

# Redis?????
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=-1ms

# Cache Configuration (???????????)
spring.cache.type=redis
spring.cache.redis.time-to-live=600000

# ==================== AI Configuration ====================
# OpenAI API Settings
# Set your API key via environment variable: SPRING_AI_OPENAI_API_KEY
spring.ai.openai.api-key=${SPRING_AI_OPENAI_API_KEY:}
spring.ai.openai.base-url=${SPRING_AI_OPENAI_BASE_URL:https://api.openai.com}

# Chat Model Settings
spring.ai.openai.chat.options.model=${SPRING_AI_OPENAI_MODEL:gpt-4o-mini}
spring.ai.openai.chat.options.temperature=0.7
spring.ai.openai.chat.options.max-tokens=2000

# AI Feature Toggle (set to false to disable AI features when API key is not configured)
app.ai.enabled=${AI_ENABLED:true}
